import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
const ts = { createdAt: integer("created_at", { mode: "timestamp_ms" }).notNull(), updatedAt: integer("updated_at", { mode: "timestamp_ms" }).notNull(), deletedAt: integer("deleted_at", { mode: "timestamp_ms" }) };

export const users = sqliteTable("users", { id: text("id").primaryKey(), email: text("email").notNull(), nickname: text("nickname").notNull(), profileImageUrl: text("profile_image_url"), ...ts });
export const userRoles = sqliteTable("user_roles", { id: text("id").primaryKey(), userId: text("user_id").notNull(), role: text("role").notNull(), grantedBy: text("granted_by"), grantedAt: integer("granted_at", { mode: "timestamp_ms" }).notNull(), revokedAt: integer("revoked_at", { mode: "timestamp_ms" }), ...ts });
export const equipments = sqliteTable("equipments", { id: text("id").primaryKey(), userId: text("user_id").notNull(), category: text("category").notNull(), name: text("name").notNull(), usageMetricType: text("usage_metric_type").notNull(), usageMetricValue: integer("usage_metric_value").notNull(), visibility: text("visibility").notNull(), moderationStatus: text("moderation_status").notNull(), isOperationalHidden: integer("is_operational_hidden", { mode: "boolean" }).notNull().default(false), ...ts });
export const equipmentPhotos = sqliteTable("equipment_photos", { id: text("id").primaryKey(), equipmentId: text("equipment_id").notNull(), storageKey: text("storage_key").notNull(), caption: text("caption"), isCover: integer("is_cover", { mode: "boolean" }).notNull().default(false), visibility: text("visibility").notNull(), moderationStatus: text("moderation_status").notNull(), ...ts });
export const maintenanceLogs = sqliteTable("maintenance_logs", { id: text("id").primaryKey(), equipmentId: text("equipment_id").notNull(), title: text("title").notNull(), body: text("body"), usageMetricValue: integer("usage_metric_value"), visibility: text("visibility").notNull(), moderationStatus: text("moderation_status").notNull(), ...ts });
export const parts = sqliteTable("parts", { id: text("id").primaryKey(), equipmentId: text("equipment_id").notNull(), name: text("name").notNull(), brand: text("brand"), visibility: text("visibility").notNull(), moderationStatus: text("moderation_status").notNull(), ...ts });
export const reminders = sqliteTable("reminders", { id: text("id").primaryKey(), equipmentId: text("equipment_id").notNull(), label: text("label").notNull(), dueAt: integer("due_at", { mode: "timestamp_ms" }), isDone: integer("is_done", { mode: "boolean" }).notNull().default(false), ...ts });
export const themes = sqliteTable("themes", { id: text("id").primaryKey(), name: text("name").notNull(), code: text("code").notNull(), isVisible: integer("is_visible", { mode: "boolean" }).notNull().default(true), ...ts });
export const sitePages = sqliteTable("site_pages", { id: text("id").primaryKey(), slug: text("slug").notNull(), title: text("title").notNull(), status: text("status").notNull(), ...ts });
export const siteSections = sqliteTable("site_sections", { id: text("id").primaryKey(), pageId: text("page_id").notNull(), type: text("type").notNull(), title: text("title").notNull(), body: text("body"), image: text("image"), cta: text("cta"), sortOrder: integer("sort_order").notNull().default(0), isVisible: integer("is_visible", { mode: "boolean" }).notNull().default(true), ...ts });
export const banners = sqliteTable("banners", { id: text("id").primaryKey(), title: text("title").notNull(), description: text("description"), ctaLabel: text("cta_label"), linkUrl: text("link_url"), sortOrder: integer("sort_order").notNull().default(0), isVisible: integer("is_visible", { mode: "boolean" }).notNull().default(true), ...ts });
export const notices = sqliteTable("notices", { id: text("id").primaryKey(), title: text("title").notNull(), body: text("body").notNull(), status: text("status").notNull(), isPinned: integer("is_pinned", { mode: "boolean" }).notNull().default(false), publishedAt: integer("published_at", { mode: "timestamp_ms" }), ...ts });
export const faqItems = sqliteTable("faq_items", { id: text("id").primaryKey(), question: text("question").notNull(), answer: text("answer").notNull(), sortOrder: integer("sort_order").notNull().default(0), isPublished: integer("is_published", { mode: "boolean" }).notNull().default(false), ...ts });
export const boards = sqliteTable("boards", { id: text("id").primaryKey(), slug: text("slug").notNull(), title: text("title").notNull(), status: text("status").notNull(), permission: text("permission").notNull(), ...ts });
export const posts = sqliteTable("posts", { id: text("id").primaryKey(), boardId: text("board_id").notNull(), authorId: text("author_id").notNull(), title: text("title").notNull(), body: text("body").notNull(), status: text("status").notNull(), visibility: text("visibility").notNull(), moderationStatus: text("moderation_status").notNull(), ...ts });
export const comments = sqliteTable("comments", { id: text("id").primaryKey(), postId: text("post_id").notNull(), authorId: text("author_id").notNull(), body: text("body").notNull(), status: text("status").notNull(), moderationStatus: text("moderation_status").notNull(), ...ts });
export const reports = sqliteTable("reports", { id: text("id").primaryKey(), resourceType: text("resource_type").notNull(), resourceId: text("resource_id").notNull(), reporterId: text("reporter_id").notNull(), reason: text("reason").notNull(), status: text("status").notNull(), ...ts });
export const moderationActions = sqliteTable("moderation_actions", { id: text("id").primaryKey(), reportId: text("report_id"), moderatorId: text("moderator_id").notNull(), action: text("action").notNull(), note: text("note"), ...ts });
export const adminAuditLogs = sqliteTable("admin_audit_logs", { id: text("id").primaryKey(), adminUserId: text("admin_user_id").notNull(), action: text("action").notNull(), resourceType: text("resource_type").notNull(), resourceId: text("resource_id").notNull(), beforeValueJson: text("before_value_json"), afterValueJson: text("after_value_json"), reason: text("reason"), ipAddress: text("ip_address"), userAgent: text("user_agent"), createdAt: integer("created_at", { mode: "timestamp_ms" }).notNull() });
