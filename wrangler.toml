name = "maniac-garage"
compatibility_date = "2026-05-28"
pages_build_output_dir = "out"

# Cloudflare Pages reads production/preview bindings from the dashboard.
# Configure the D1 binding in:
# Pages project > Settings > Bindings > D1 database
# Binding name: DB
# Database: maniac-garage-dev
#
# Do not keep placeholder database_id values here; Pages deploy validates them.
# R2 binding will be added after the upload flow is implemented.
